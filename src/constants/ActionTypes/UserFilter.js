import keyMirror from "keymirror";

const ActionTypes = keyMirror({
  USER_FILTER_SYNC: null,
  USER_FILTER_SYNC_REQUESTED: null,
  USER_FILTER_CHANGED: null,
});

export default ActionTypes;
